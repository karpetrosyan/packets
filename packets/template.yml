

handlers:
  iplogger:
    filters:
      layer2:
        proto: icmp
        filter-functions:
          - "filter_functions.layer2_filter"
          - "filter_functions.layer2_filter1"
        callback: "callbacks.layer2_callback"
      layer3:
        filter-functions:
          - "checkIP"
        callback: "callbacks.layer2_callback"
      callback: 'callback'
